/**
 * Esse componente é responsável por armazenar posições 2D
 *
 * @typedef {object} Position
 * @property {number} col=`0` Posição da coluna
 * @property {number} row=`0` Posição da linha
 */
const Position = { column: 0, row: 0 };

export default Position;
